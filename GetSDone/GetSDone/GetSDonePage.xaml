<?xml version="1.0" encoding="utf-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" xmlns:local="clr-namespace:GetSDone" x:Class="GetSDone.GetSDonePage">

    <ListView x:Name="todoListView"  RowHeight="100">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <ContentView >
                        <Entry Focused="Handle_Focused" 
                        BackgroundColor="{Binding BackgroundColor}"
                               TextColor="{Binding ForegroundColor}"
                               Text="{Binding Text, Mode=TwoWay}"/>
                        
                    </ContentView>
               </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>

</ContentPage>
